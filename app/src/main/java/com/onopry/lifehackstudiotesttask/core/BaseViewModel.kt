package com.onopry.lifehackstudiotesttask.core

import androidx.lifecycle.ViewModel
import kotlinx.coroutines.flow.MutableStateFlow
import kotlinx.coroutines.flow.asStateFlow

/*
class BaseViewModel<T>() : ViewModel() {
    private val screenStateMutableFlow = MutableStateFlow<ScreenState<T>>(ScreenState.Empty<T>)
    val companiesState get() = companiesMutableStateFlow.asStateFlow()
}*/
